pipeline {
    agent any

    tools {
        jdk 'JavaLocal' 
        maven 'LocalMaven'
    }
    stages {
        stage('Build') {
            steps {
                bat 'mvn clean install'
            }
        }
    }
}
