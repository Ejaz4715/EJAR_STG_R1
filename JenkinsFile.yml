pipeline {
    agent any

    tools {
        jdk 'JavaLocal' 
        maven 'LocalMaven'
    }
        stage('Build') {
            steps {
                bat 'mvn clean install'
            }
        }

    }
