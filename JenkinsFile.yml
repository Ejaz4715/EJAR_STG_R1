pipeline {
    agent any

    tools {
        jdk 'JavaLocal' 
        maven 'MavenLocal'
    }
    stages {
        stage('Build') {
            steps {
                bat 'mvn clean install'
            }
        }
    }
}
